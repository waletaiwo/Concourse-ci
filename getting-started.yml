jobs: 
- name: getting-started-job
  plan:
  - task: getting-started-task
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: babz/concourse:0.10
      run:
        path: echo
        args: ["Welcome to concourse Babz"]
